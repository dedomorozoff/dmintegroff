{
  "name": "PostgreSQL",
  "description": "SQL запрос для вставки в PostgreSQL",
  "type": "text",
  "category": "databases",
  "template": "INSERT INTO users (name, email, phone, metadata, created_at) VALUES ('{{user.name}}', '{{user.email}}', '{{user.phone}}', '{{metadata}}', CURRENT_TIMESTAMP) RETURNING id;"
}