{
  "name": "Stripe",
  "description": "Создание платежа в Stripe",
  "type": "json",
  "category": "payments",
  "template": {
    "amount": "{{payment.amount}}",
    "currency": "rub",
    "description": "{{payment.description}}",
    "customer": {
      "email": "{{user.email}}",
      "name": "{{user.name}}",
      "phone": "{{user.phone}}"
    },
    "metadata": {
      "user_id": "{{user.id}}",
      "order_id": "{{order.id}}"
    }
  }
}